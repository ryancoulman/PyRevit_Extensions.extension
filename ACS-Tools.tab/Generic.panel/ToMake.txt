TO MAKE 
- Hide all sections not on sheet 
- Apply filter(s) to selected views on sheet 
    - (in sheet view) Form to select filter(s) and a button to turn visibility on or off 
    - If colour overides involved option to copy a filter from current view to others on sheet 
    - Use functiosn from Revit Links to get all views on sheet, and Overide All Filters for copying behavior 
- Expand 'Batch Filters' to Annotation Cats, Revit links, Model Cats so users can batch apply these 
- Custom script builder to allow user to create keyboard shortcuts to turn selected worksets on / off (nned individual script to assign KS too)
    - Some kind of pushsplitpulldown button that has a button to create a script which it adds to the pulldown 
    - The scripts can then simply just call some classes in a lib folder to handle hiding the worsket (just pass name / ID accross)
    - Could look at dev tab tool that allows you to create pushbuttons etc
    - Extend beyond worksets
- “idle-based autosave” in Revit:
    - Case A: User switches away from Revit (window loses focus) → trigger autosave.
    - Case B: User is still in Revit but inactive for some threshold t (no mouse/keyboard input) → trigger autosave.
    - Case C: If t1 < t < t2 (where tn = time since last save) then just autosave anyway 
- “Why is Element Hidden in View?” Diagnostic Tool
    - A diagnostic command that checks common visibility blockers (hidden in view, worksets, filters, phases, 
      category visibility, crop/view range, etc.).
    - Returns a clear list of possible reasons why a selected element is invisible in the current view.
- "Sync and shut down" + "Sync, Export, (optional) Shutdown" 
    - You could potentially create a script that monitors Revit's journal file for sync completion messages, then triggers a shutdown easily through os module 
- 'Cross-program Revit ↔ Navisworks selection tool'
    - Goal: Select an element in Revit and instantly highlight the same element in Navisworks.
    - Revit add-in: Capture Element.UniqueId when user selects an element.
    - Bridge: Send UniqueId to Navisworks (via JSON file, named pipe, or socket).
    - Navisworks add-in: Listen for incoming UniqueId → search ModelItem.PropertyCategories for matching “Revit GUID” → select & zoom in Navisworks viewer.
- Simple but useful: 'Select all tags of selected families and vice versa). useful when tagging and moving tags. can be easier to select for one 

--Meta--
Insted of having dropdowns on some buttons as if they want to apply to selected elements, elements in view, elements in multiple views you should create a meta class that checks if
- user has selected any view in project browser 
- user has selected elements 
- else just apply to active view 
Then run default script 
Need option for views in sheet vs 2d sheet AND search entire project 
